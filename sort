

// Sort requests
For (int I = 0; I &lt; n – 1; i++) {
For (int j = 0; j &lt; n – I – 1; j++) {
If (requests[j] &gt; requests[j + 1]) {
Int temp = requests[j];
Requests[j] = requests[j + 1];
Requests[j + 1] = temp;
}
}
